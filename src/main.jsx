import React from 'react';
import ReactDOM from 'react-dom/client';
import SmartFilmPage from '../SmartFilmPage';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SmartFilmPage />
  </React.StrictMode>
);
